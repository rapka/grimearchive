doctype html
html
	head
		title= title
		link(rel='stylesheet', href='/style/style.css')
		link(rel='shortcut icon', href='/img/favicon.ico')
	body
		.title-pic
			img(src='/img/title.png')
		.nav-bar
			ul
				li
					a(href='/') Home
				li
					a(href='/mixes/') Mixes
				li
					a(href='/upload') Upload
				li
					a(href='/about') About
	
	block content

	div.footer
		script(src='https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js')
		script(type='text/javascript').

			var updateQuote = function(event) {
				$.ajax({
					url: '/quote',
					type: 'GET',
					success: function(data){
					var oldquote = $('.quote').text();
					var newquote = '"' + data + '"';
					$('.quote').text(newquote);

					//re-run to avoid not sending a new quote
					if (newquote == oldquote){
						updateQuote();
						}
					}
				});
			}

			$(function(){
				updateQuote();
				$('.quote').on('click', updateQuote);
			});
		div.quote
		p © 2015 College Hill. Mixes are hosted for non-profit archival use only. Contact thecollegehill@gmail.com for copyright concerns. 