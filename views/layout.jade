doctype html
html
	head
		title= title
		meta(charset='utf-8')
		meta(name="viewport" content="width=device-width, initial-scale=1.0")
		link(rel='stylesheet', href='/style/style.css')
		link(rel='shortcut icon', href='/img/favicon.ico')
	body
		//- Google Tag Manager
		noscript
			iframe(src="//www.googletagmanager.com/ns.html?id=GTM-P9LX7W" height="0" width="0" style="display:none;visibility:hidden")
		script.
			(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
			new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
			j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
			'//www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
			})(window,document,'script','dataLayer','GTM-P9LX7W');
		//- End Google Tag Manager

		.title-pic.big.red
			a(href='/') The Grime Archive.
		div.nav-bar.medium
			ul
				li
					.blue
						a(href='/') Home
				li
					.yellow
						a(href='/mixes/page/1') Mixes
				li.upload-nav
					.red
						a(href='/upload') Upload
				li
					.green
						a(href='/about') About
				li.searchbar
					form(name="search", action="/search", method="get", enctype='multipart/form-data')
						input(type="text", name="title")
						input(type='submit', value='Search >> ')
				li.advanced-layout
					.blue
						a(href='/advanced') [Advanced]
				li.mobile-search
					.red
						a(href='/advanced') Search
	
	block content

	div.footer
		br
		script(src='https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js')
		script(type='text/javascript').

			var updateQuote = function(event) {
				$.ajax({
					url: '/quote',
					type: 'GET',
					success: function(data){
					var oldquote = $('.quote').text();
					var newquote = '"' + data + '"';
					$('.quote').text(newquote);

					//re-run to avoid not sending a new quote
					if (newquote == oldquote){
						updateQuote();
						}
					}
				});
			}

			$(function(){
				updateQuote();
				$('.quote').on('click', updateQuote);
			});
		div.quote
		div.disclaimer
			p Site © 2015 College Hill. Mixes are hosted for non-profit archival use only. Contact thecollegehill@gmail.com with any questions or concerns. 